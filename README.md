# graph_ql_tutorial
graph qlの勉強ちゃーーーーー

リポジトリ直下に`env-config.json`を作ってね。



形式は

```json
{
    "environment": string,
    "host": {
        "protocol": string,
        "hostname": string,
        "port": number
    }
}
```

だよ！！

